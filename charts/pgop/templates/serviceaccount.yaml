{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pgop.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pgop.labels" . | nindent 4 }}
{{- end }}
