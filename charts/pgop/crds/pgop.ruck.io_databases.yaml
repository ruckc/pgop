---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: databases.pgop.ruck.io
spec:
  group: pgop.ruck.io
  names:
    kind: Database
    listKind: DatabaseList
    plural: databases
    singular: database
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.owner
      name: Owner
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Database is the Schema for the databases API.
          It represents a PostgreSQL database managed by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Database
            properties:
              clusterRef:
                description: clusterRef references the PostgreSQL Cluster this database
                  belongs to
                properties:
                  name:
                    description: name is the name of the Cluster resource in the same
                      namespace
                    type: string
                required:
                - name
                type: object
              extensions:
                description: extensions lists PostgreSQL extensions to install in
                  this database
                items:
                  description: ExtensionSpec defines a PostgreSQL extension to install
                  properties:
                    name:
                      description: name is the name of the PostgreSQL extension
                      type: string
                    schema:
                      description: |-
                        schema is the schema to install the extension into.
                        If not specified, the extension is installed into the default schema.
                      type: string
                    version:
                      description: |-
                        version is the version of the extension to install.
                        If not specified, the latest available version is installed.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              owner:
                description: |-
                  owner is the role that owns this database.
                  If not specified, the operator superuser will be the owner.
                type: string
              schemas:
                description: schemas lists schemas to create in this database
                items:
                  description: SchemaSpec defines a schema to create in the database
                  properties:
                    grants:
                      description: grants lists privileges to grant on this schema
                      items:
                        description: GrantSpec defines privileges to grant to a role
                        properties:
                          privileges:
                            description: privileges lists the privileges to grant
                              (e.g., USAGE, CREATE, ALL)
                            items:
                              type: string
                            minItems: 1
                            type: array
                          role:
                            description: role is the role to grant privileges to
                            type: string
                          withGrantOption:
                            description: withGrantOption allows the grantee to grant
                              the same privileges to others
                            type: boolean
                        required:
                        - privileges
                        - role
                        type: object
                      type: array
                    name:
                      description: name is the name of the schema
                      type: string
                    owner:
                      description: |-
                        owner is the role that owns this schema.
                        If not specified, the database owner will be the schema owner.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: status defines the observed state of Database
            properties:
              conditions:
                description: conditions represent the current state of the Database
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdSchemas:
                description: createdSchemas lists schemas that have been successfully
                  created
                items:
                  type: string
                type: array
              installedExtensions:
                description: installedExtensions lists extensions that have been successfully
                  installed
                items:
                  type: string
                type: array
              ready:
                description: ready indicates if the database has been created
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
